import Foundation
import Testing
@testable import Spread

/// SpreadHierarchyOrganizer was removed when traditional mode was deleted.
/// Traditional mode used a year → month → day hierarchy for navigation.
/// Conventional mode uses the SpreadHeaderNavigatorModel instead.
struct SpreadHierarchyTests {}
